(ns {{root}}.{{type}}s.{{name}}-test
  (:require [midje.sweet :refer :all]
            [{{root}}.{{type}}s.{{name}} :refer :all]))

(facts "Exercise {{name}}"
       (fact "basic stuff"
             (+ 2 2) => 4))
